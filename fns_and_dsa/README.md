# Functions, Data Structures & Modules — Week Project

**Repository:** `alx_be_python`  
**Directory:** `fns_and_dsa`  
**Files:** `main.py` (provided), `shopping_list_manager.py`, `explore_datetime.py`, `temp_conversion_tool.py`

---

## 🚀 Overview
This mini-suite reinforces Python fundamentals through a single, menu-driven app:
- **Functions**: parameters, return values, scope, clear responsibilities.
- **Core Data Structures**: lists, sets, dicts, tuples—chosen intentionally per task.
- **Modules**: local imports, standard library usage (`datetime`), simple project layout.

`main.py` is the entry point and routes to the tools contained in this same project folder.

---

## 🎯 Learning Objectives
- Define & use functions (`def`, simple lambdas), arguments (positional/keyword), defaults, return values.
- Understand **scope & namespaces**: LEGB, avoiding globals; `global`/`nonlocal` as exceptions.
- Choose appropriate **data structures**: ordering, uniqueness, lookup, mutability.
- Use **dict/list/set** methods; understand tuple immutability & hashability.
- Work with **modules & packages**; import stdlib; install and use external libs (`pip`) when needed.
- Apply best practices for readable, testable, maintainable code with clean control flow.

---

## 📁 Project Structure

```text
alx_be_python/
└─ fns_and_dsa/
├─ README.md
├─ main.py # provided entry point / menu router
├─ shopping_list_manager.py # add/remove/list/clear items
├─ explore_datetime.py # current time, formatting, deltas
└─ temp_conversion_tool.py # Celsius <-> Fahrenheit conversions
```


---

## 🧰 Requirements & Setup
- Python **3.10+** (required for `match/case`)
- (Optional) Virtual environment

**Setup**

```bash
cd alx_be_python/fns_and_dsa
python -m venv .venv
# Windows: .venv\Scripts\activate
# Linux/macOS:
source .venv/bin/activate
python -m pip install --upgrade pip
---

## 📄 License (MIT)

Copyright © 2025 Mayo Takémsi Norris KADANGA